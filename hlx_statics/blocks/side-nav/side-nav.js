import {
  removeEmptyPTags,
  decorateButtons,
  createTag,
  applyAnalyticHeaderOverride,
} from '../../scripts/lib-adobeio.js';

/**
 * decorates the side-nav
 * @param {Element} block The site-nav block element
 */
export default async function decorate(block) {
 console.log('side naving')
 console.log(block)
}